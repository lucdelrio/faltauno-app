<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.faltauno">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/falta_uno"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/falta_uno"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:icon="@mipmap/falta_uno"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActivityOne" />
        <activity android:name=".activities.ActivityTwo" />
        <activity android:name=".activities.ActivityMatchCreation"></activity>
    </application>

</manifest>